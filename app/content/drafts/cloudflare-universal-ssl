curl https://api.cloudflare.com/client/v4/zones/ZONE/ssl/verification \
    -H "X-Auth-Email: EMAIL" \
    -H "X-Auth-Key: KEY"

curl https://api.cloudflare.com/client/v4/zones/ZONE/ssl/universal/settings \
    -H "X-Auth-Email: EMAIL" \
    -H "X-Auth-Key: KEY"


curl https://api.cloudflare.com/client/v4/zones/ZONE/ssl/verification/KEY \
    -X PATCH \
    -H 'Content-Type: application/json' \
     -H "X-Auth-Email: EMAIL" \
    -H "X-Auth-Key: KEY" \
    -d '{
      "validation_method": "cname"
    }'

curl https://api.cloudflare.com/client/v4/zones/ZONE/ssl/verification/KEY \
    -X PATCH \
    -H 'Content-Type: application/json' \
     -H "X-Auth-Email: EMAIL" \
    -H "X-Auth-Key: KEY" \
    -d '{
      "validation_method": "cname"
    }'



    https://developers.cloudflare.com/ssl/edge-certificates/universal-ssl/enable-universal-ssl/

    https://developers.cloudflare.com/ssl/edge-certificates/changing-dcv-method/methods/


    https://developers.cloudflare.com/api/resources/ssl/subresources/universal/

    https://developers.cloudflare.com/ssl/reference/certificate-authorities/#caa-records

    https://developers.cloudflare.com/ssl/edge-certificates/universal-ssl/limitations/

    > For Universal SSL certificates, Cloudflare chooses the certificate authority (CA) used for your certificate.

> Cloudflare can change the certificate authority without prior notification, and will not send any notification as the change happens.

> If you want to choose the issuing certificate authority, order an advanced certificate.