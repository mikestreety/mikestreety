content:
  - name: notes
    label: Notes
    path: app/content/notes
    type: collection
    format: yaml-frontmatter
    subfolders: false
    fields:
      - name: title
        label: Title
        type: string
      - name: body
        label: Body
        type: rich-text
    view:
      fields: [ title, date ]
      primary: title
      sort: [ date, title ]
      default:
        search: ''
        sort: date
        order: desc